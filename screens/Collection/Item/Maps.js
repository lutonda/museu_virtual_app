import React, { Component } from 'react'
import {
   View, MapView, Marker,
   StyleSheet
} from 'react-native'

export default Maps = (props) => {
   return (
      <MapView
      />
   )
}

const styles = StyleSheet.create({
   map: {
      height: 400,
      marginTop: 80
   }
})